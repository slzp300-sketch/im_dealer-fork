/**
 * 워커 프로토콜 버전.
 *
 * 담당자 PC 는 zip 으로 코드를 받으므로, 백엔드만 배포되고 워커는 옛 코드로
 * 계속 도는 상황이 생긴다. 실제로 "자격증명을 받지 않도록" 바꿨을 때
 * 옛 워커가 그 작업을 집어가 엉뚱한 이유로 실패한 사례가 있었다.
 *
 * 백엔드와 워커가 이 값을 비교해, 다르면 워커가 작업을 가져가지 않는다.
 *
 * ── 언제 올리나 ──
 * 워커와 서버가 주고받는 규약이 바뀌어 옛 워커가 오작동할 수 있을 때만 올린다.
 *   예) claim 응답 필드 추가/의미 변경, 결과 페이로드 형식 변경,
 *       자격증명 유무 규칙 변경
 * 어댑터 내부 수정처럼 규약과 무관한 변경은 올리지 않아도 된다
 * (옛 워커도 정상 동작하며, 다음 zip 배포 때 자연히 따라온다).
 * 단, 옛 코드가 오작동을 일으키는 중대한 어댑터 수정은 run.ps1 자동 업데이트를
 * 발동시키기 위해 올린다 — v5: 우리금융 세션 차단(rentRemain 예산) 우회 배포.
 */
export const WORKER_PROTOCOL_VERSION = 5;
